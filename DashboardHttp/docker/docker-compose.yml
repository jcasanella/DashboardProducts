version: '2'
services:
  dbpostgres:
    image: postgres
    volumes:
      - /home/jordi/docker/volumes/postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: mytestdb